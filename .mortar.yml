branches:
  only:
    - master
language: java
jdk: oraclejdk7
before_install: "ls -la ; pwd"
install: mvn clean install
script: "ls -laR"
jenkins:
  unstable: true
  collect:
    artifacts: 'target/*.jar'
